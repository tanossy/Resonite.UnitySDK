# 変更点まとめ 2026-07-12（commit 4e189f30）

ブランチ: `feature/baked-lightmap-import`
親コミット: `d2eff895`（方向性ライトマップ＋Filamented/Standard_Culloff対応）

Resonite実機オフライン中に、アテナ（ダイダロス/ロキ経由）とポセイドン（Codex）が並行して作業した内容の記録。混在コミットになっているため、誰が何を変えたかをここに残す。

---

## アテナ側（ダイダロス実装／ロキ監査）

### 新規: PostProcessingConverter.cs
`Assets/ResoniteSDK/ComponentConverters/Unity Core/Rendering/PostProcessingConverter.cs`

Unity Post Processing Stack v2（PPv2、Globalボリューム限定）→ Resonite `PostProcessingSettings`（5フィールドのみ）へ変換。

| Unity側 | Resonite側 |
|---|---|
| `Bloom.intensity` | `BloomIntensity` |
| `AmbientOcclusion.intensity` | `AmbientOcclusionIntensity` |
| `MotionBlur.shutterAngle/360` | `MotionBlurIntensity`（近似） |
| `ScreenSpaceReflections.enabled` | `ScreenSpaceReflections` |
| `PostProcessLayer.antialiasingMode`（Camera側） | `Antialiasing` |

色調補正/Vignette/DOF/露出はResonite側に対応フィールドが無いため非対応（意図的）。**要別途Package Manager導入**（このコミットで`Packages/manifest.json`に`com.unity.postprocessing`を追加済みだが、ライブプロジェクト側は未導入）。

### 新規: AudioEffectConverter.cs
`Assets/ResoniteSDK/ComponentConverters/Unity Core/Audio/AudioEffectConverter.cs`

Unity `AudioReverbFilter`/`AudioEchoFilter` → Resonite `AudioZitaReverb`へベストエフォート変換。Zita-Rev1パラメータ名一致からの推定実装（**2026-07-12午後の追加調査で公式wiki記載を確認、Zita-Rev1対応は確定。`PresetUnderwater`等のプリセットメソッドの活用は未実装、次フェーズ候補**）。

### 修正: DirectionalLightmapBaker.cs / LightmapMaterialCache.cs / LightmapDecoder.cs / Uv2NormalPatch.shader / (Poiyomi側)PoiyomiBakeStandin.cs

ロキのコードレビューで発見した実害バグ5件を修正:
1. **識別子衝突（高）**: `GetRendererIdentity`が名前パスのみに依存→同名オブジェクトでライトマップ相互上書き。`GlobalObjectId.GetGlobalObjectIdSlow`に置換
2. **normalPatchのV反転（中〜高）**: 手動`_ProjectionParams.x`補正を撤去、実証済み`BlitReadableAtlas`経路に統一
3. **nullフォールバック未実装（中）**: `LightmapMaterialCache`でデコード失敗時に`source`へ戻さずnullテクスチャのまま変種生成していたのを修正
4. **PoiyomiBakeStandin復元の頑健性（低〜中）**: `RestoreAll`ループにtry/catch追加
5. **HDRクリッピング診断（中〜高、実測確定済みの実害）**: `LightmapDecoder`にmaxChannel診断ログ追加

### 変更: BakedLightmapStandardConverter.cs / MeshRendererConverter.cs / SkinnedMeshRendererConverter.cs
上記バグ修正・Filamented/Standard_Culloff対応に伴う小規模な追随変更（フォークdiffベース、詳細はgit log参照）。

---

## ポセイドン側（並行作業・Codex）

### 新規: ConversionPassState.cs
`Assets/ResoniteSDK/ConversionPassState.cs`

```csharp
public enum ResoniteSdkConversionPass { Full, MeshesOnly, MaterialsOnly }
public static class ConversionPassState {
    public static ResoniteSdkConversionPass ActivePass { get; set; } = Full;
    public static bool ShouldConvertMeshes => ActivePass != MaterialsOnly;
    public static bool ShouldConvertMaterials => ActivePass != MeshesOnly;
}
```

**部分変換パス機構**。「メッシュだけ送る」「マテリアルだけ送る」をシーン全体の再送信無しで行えるようにする仕組み。今夜のOceanView送信で音声変換中にWebSocket切断が起きた反省（大規模シーンの一括送信は時間がかかりすぎる）に直接効く改善。

### 変更: ResoniteLinkWindow.cs
SDK Managerパネルに新規UI追加:
- **「Send Meshes Only」** / **「Send Materials Only」** ボタン（`ConversionPassState`を切り替えて`SceneConverter`を呼ぶ）
- **「Retry Missing Asset URLs」** ボタン
- **「Force Refresh Generated Lightmaps」** トグル（`ForceRefreshGeneratedLightmaps`、既定true）

### 変更: SkyboxConverter.cs
`[SerializeReference] cannot serialize objects when the field type is a generic with supplied type parameters, in this case FrooxEngine.Sync<SphericalHarmonicsL2>`エラーの修正。`ConvertSphericalHarmonics`（既定false）フラグでSH2アンビエントライト変換経路を切り離し、Skybox/ReflectionProbe本体の変換は維持。

### 変更: AssetConverter.cs / Texture2DConverter.cs / MeshColliderConverter.cs / AssetConversionManager.cs / SceneConverter.cs / StandardBaseConverter.cs / StandardConverter.cs / StandardSpecularConverter.cs
`ConversionPassState.ShouldConvertMeshes`/`ShouldConvertMaterials`を各コンバータの分岐に組み込み、部分変換パス機構に対応させる変更（詳細差分は`git log -p`参照）。

---

## 次フェーズでの検討事項（ブランチ分離後）

- AudioEffectConverterへの`PresetUnderwater`等プリセット活用の組み込み検討
- 水表現（PBS+Panner2D）の実装検討（レシピは泉に記録済み、コード未着手）
- ライブプロジェクト側へのPPv2パッケージ導入
- ポセイドンの部分変換パス機構（Send Materials Only）を使った、大規模シーンでの再送信フロー確立

## 関連

- 泉: `C:/urd/wiki/concepts/resonite/dev-recipes/2026-07-12_rezo_con_unity_resonite_extended_converters.md`
- メモリ: `project_bakery_lightmap_to_resonite.md` / `project_codex_collaboration.md`
